# ── MongoDB Connection (optional — app falls back to in-memory if unavailable)
MONGO_URI=mongodb://localhost:27017
MONGO_DB=smart_campus

# ── Flask Server ────────────────────────────────────────────
FLASK_HOST=0.0.0.0
FLASK_PORT=5000
